<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huge.web.mapper.UserMapper">

<select id="findUserById" parameterType="java.lang.Integer" resultType="com.huge.web.pojo.User">
	select * from user_tb where id=#{id}
</select>

<select id="findUser" parameterType="java.lang.String" resultType="com.huge.web.pojo.User">
	select * from user_tb where realname=#{realname}
</select>

</mapper>








